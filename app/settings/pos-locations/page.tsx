import { PosLocationsView } from "@/components/settings/locations-view";

export const metadata = { title: "POS Locations — Marina Stee Settings" };

export default function PosLocationsPage() {
  return <PosLocationsView />;
}
